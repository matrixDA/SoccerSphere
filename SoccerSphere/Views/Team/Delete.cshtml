@model PlayerTeamViewModel
@{
    ViewBag.Title = "Delete - " + Model.CurrentPlayer.PlayerName;
}


<div class="card shadow-lg mt-4">
        <div class="card-header bg-danger text-white fs-3">
            Confirm Deletion
        </div>
        <div class="card-body p-4" style="font-size: 1.25rem;">
            <h3 class="card-title">@Model.CurrentPlayer.PlayerName - @Model.CurrentPlayer.Team.TeamName</h3>
            <p class="card-text text-muted">
                Are you sure you want to delete this player? This action cannot be undone.
            </p>

            <form asp-action="Delete" method="post" class="mt-4">
                <input type="hidden" asp-for="CurrentPlayer.PlayerId" />

                <div class="d-flex justify-content-start gap-3">
                    <button type="submit" class="btn btn-danger btn-lg">Delete</button>
                    <a asp-controller="Team" asp-action="Players" class="btn btn-secondary btn-lg">Cancel</a>
                </div>
            </form>
        </div>
    </div>
