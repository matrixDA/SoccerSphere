@model PlayerTeamViewModel
@{
    ViewBag.Title = "Add New Player";
}

<h2>Add New Player</h2>

<form asp-controller="Team" asp-action="Add" method="post">

    <div asp-validation-summary="All" class="text-danger"></div>

    <div class="form-group">
        <label asp-for="@Model.CurrentPlayer.PlayerName" class="form-label"></label>
        <input asp-for="@Model.CurrentPlayer.PlayerName" class="form-control" />
        <span asp-validation-for="@Model.CurrentPlayer.PlayerName" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="@Model.CurrentPlayer.Country" class="form-label"></label>
        <input asp-for="@Model.CurrentPlayer.Country" class="form-control" />
        <span asp-validation-for="@Model.CurrentPlayer.Country" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="@Model.CurrentPlayer.TeamId" class="form-label">Team</label>
        <select asp-for="@Model.CurrentPlayer.TeamId" asp-items="@ViewBag.Teams" class="form-control">
            <option value="">-- Select a Team --</option>
        </select>
        <span asp-validation-for="@Model.CurrentPlayer.TeamId" class="text-danger"></span>
    </div>

    <div class="row">
        <div class="col-4">
            <div class="form-group">
                <label asp-for="@Model.CurrentPlayer.Goals" class="form-label"></label>
                <input asp-for="@Model.CurrentPlayer.Goals" type="number" class="form-control" />
            </div>
        </div>
        <div class="col-4">
            <div class="form-group">
                <label asp-for="@Model.CurrentPlayer.Assists" class="form-label"></label>
                <input asp-for="@Model.CurrentPlayer.Assists" type="number" class="form-control" />
            </div>
        </div>
        <div class="col-4">
            <div class="form-group">
                <label asp-for="@Model.CurrentPlayer.MatchesPlayed" class="form-label"></label>
                <input asp-for="@Model.CurrentPlayer.MatchesPlayed" type="number" class="form-control" />
            </div>
        </div>
        <div class="col-4">
            <div class="form-group">
                <label asp-for="@Model.CurrentPlayer.Rating" class="form-label"></label>
                <input asp-for="@Model.CurrentPlayer.Rating" type="number" class="form-control" />
            </div>
        </div>
    </div>

    <button type="submit" class="btn btn-primary">Add Player</button>
    <a asp-controller="Team" asp-action="Index" class="btn btn-secondary">Cancel</a>
</form>